# 프로젝트 문서 운영 규약

이 저장소의 문서는 [docutest](https://github.com/daegil/docutest) 규약을 따릅니다.
계획 → 실행 → 결과 → 회고를 남기고, 시간 순서와 의사결정 맥락을 추적합니다.

지금은 **마크다운 전용**입니다. `docs/`는 gitignore라 GitHub에 올라가지 않고, 이 기기에만 둡니다.
Docusaurus 사이트는 만들지 않았습니다. 나중에 `docs-site/`를 붙일 때 아래 5절의 사이트 설정을 쓰면 됩니다.

문서를 생성·수정하기 전에 이 파일을 읽습니다.

------------------------------------------------------------------------

## 1. 핵심 목적

- 프로젝트의 모든 계획, 실행, 의사결정, 회고를 문서로 남긴다.
- 시간 흐름과 생성 순서를 파일명으로 바로 파악할 수 있게 한다.
- Draft → Approved 워크플로우를 명확히 유지한다.
- 문서는 짧고 명확하며, 서로 연결(link)되도록 작성한다.

------------------------------------------------------------------------

## 2. 문서 구조

모든 문서는 `docs/` 아래에서 다음 구조를 따른다.

```
docs/
  00-meta/            # 개요, 규칙, 템플릿
  10-plan/
    drafts/           # 초안 계획
    approved/         # 승인된 계획
  20-execution/
    logs/             # 실행 로그
    decisions/        # 의사결정 기록(ADR)
    meetings/         # 회의록
  30-deliverables/    # 산출물 정의/정리
  40-retros/          # 회고
  90-reference/       # 참고자료/리서치/용어집
```

**정렬 방식:**
- 각 카테고리 내 문서는 **파일명 알파벳 오름차순**으로 정렬
- `YYYY-MM-DD-NNN` 형식으로 인해 자동으로 **시간순 정렬**
- `position` 또는 `sidebar_position` 필드는 사용하지 않음
- `_category_.json`은 Docusaurus용이라 지금은 두지 않음

------------------------------------------------------------------------

## 3. 프런트매터 필수 규칙

모든 문서는 아래 필드를 반드시 포함해야 한다.

```yaml
---
title: "문서 제목"
sidebar_label: "[YYYY/MM/DD-NNN] 짧은 제목"
slug: url-friendly-slug
tags:
  - plan
draft: true
---
```

`tags`는 문서 유형에 맞는 값 하나 이상: `plan` | `execution` | `decision` | `meeting` | `deliverable` | `retro` | `reference` | `meta`

### slug 규칙

파일명이 영문 kebab-case이면 slug는 파일명에서 날짜·순번을 뺀 제목과 같게 둔다.
한글·공백·특수문자가 파일명에 있으면 slug는 필수이며 영문 소문자 kebab-case만 쓴다.

### sidebar_label 규칙

형식:

```
[YYYY/MM/DD-NNN] 제목
```

예시:

```
[2026/09/18-001] 로컬 MVP
```

**순번(NNN) 규칙:**
- NNN은 **각 최상위 카테고리 내에서** 해당 날짜 기준 001부터
- 카테고리: `00-meta`, `10-plan`, `20-execution`, `30-deliverables`, `40-retros`, `90-reference`
- `20-execution`의 logs·decisions·meetings는 **같은 순번 축**을 공유한다
- 날짜 태그는 승인 후에도 바꾸지 않는다

------------------------------------------------------------------------

## 4. 파일명 규칙

```
YYYY-MM-DD-NNN-kebab-title.md
```

예시:

```
docs/20-execution/logs/2026-09-18-001-auth-claude-emotions-vercel.md
docs/20-execution/decisions/2026-09-18-002-localstorage-auth.md
```

- 날짜는 `YYYY-MM-DD`
- 순번은 항상 3자리
- 소문자 kebab-case
- 한글 파일명은 쓰지 않는다

------------------------------------------------------------------------

## 5. Mermaid 다이어그램

마크다운 코드 블록의 언어를 `mermaid`로 두면 GitHub에서 렌더링된다.
다이어그램은 본문을 보조하는 용도만 쓴다. 한글 노드 텍스트는 대괄호로 감싼다.

이후 Docusaurus를 붙일 때:

```bash
npm install --save @docusaurus/theme-mermaid
```

```typescript
export default {
  markdown: { mermaid: true },
  themes: ["@docusaurus/theme-mermaid"],
};
```

------------------------------------------------------------------------

## 6. 문서 유형별 작성 규칙

### 6.1 계획 문서 (Plan)

```
## 배경 / 문제 정의
## 목표 (측정 가능해야 함)
## 비목표 (Non-goals)
## 범위 (Scope)
## 마일스톤
## 리스크 및 대응
## 미해결 이슈
## 관련 문서 링크
```

### 6.2 실행 로그 (Execution Log)

```
## 기간 또는 날짜
## 진행 내용
## 계획 대비 상태
## 블로커 / 이슈
## 다음 액션 (담당자 + 기한)
```

### 6.3 의사결정 문서 (Decision / ADR)

```
## 배경 (Context)
## 결정 사항 (Decision)
## 고려한 대안 (Alternatives)
## 영향 및 결과 (Consequences)
## 후속 작업 (Follow-ups)
```

### 6.4 회의록 (Meeting Notes)

```
## 참석자
## 안건
## 논의 내용
## 결정 사항
## 액션 아이템 (담당자 + 기한)
```

------------------------------------------------------------------------

## 7. Draft → Approved 워크플로우

### Draft

- 위치: `10-plan/drafts/`
- `draft: true`

### 승인

1. `10-plan/approved/`로 이동
2. `draft: false`
3. 필요 시 `approved` 태그 추가
4. 실행 문서에서 해당 계획을 링크
5. sidebar의 날짜 태그는 바꾸지 않음

------------------------------------------------------------------------

## 8. 문서 작성 원칙

- 문서 상단에 3~6줄 요약
- 긴 문장은 피하고 불릿 중심
- 섹션 구조를 유지
- 액션: `- [ ] 작업 내용 — 담당자 @이름 — 기한 YYYY-MM-DD`
- 앱 동작의 단일 기준은 [README.md](README.md). 문서는 히스토리와 결정만 남기고 중복하지 않는다.
- `.env` 값, API 키, 비밀번호는 쓰지 않는다.

------------------------------------------------------------------------

## 9. 문서 간 링크 규칙

- 다른 문서를 참조할 때는 상대 경로 링크
- 의사결정이 있으면 Decision 문서를 링크
- 중복 내용 작성 금지
- 공통 내용은 reference로 분리 후 링크

------------------------------------------------------------------------

## 10. 금지 사항

- 날짜 또는 순번을 임의로 생성
- sidebar_label 형식 변경
- 동일 내용을 여러 문서에 중복 작성
- Draft 문서를 승인 폴더로 옮기면서 `draft: true` 유지
- 시크릿을 문서에 붙이기

------------------------------------------------------------------------

## 11. 문서 생성 시 기본 체크리스트

- [ ] 올바른 폴더에 생성했는가
- [ ] 파일명이 규칙을 따르는가
- [ ] sidebar_label 형식이 맞는가
- [ ] slug가 있는가
- [ ] tags가 최소 1개 이상인가
- [ ] draft 상태가 맞는가
- [ ] 필수 섹션이 모두 포함되었는가
